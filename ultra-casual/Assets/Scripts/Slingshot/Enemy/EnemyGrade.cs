public enum EnemyGrade
{
    Weak0 = 0,      // dies on light bumps
    Weak1 = 1,      // dies on light bumps
    Weak2 = 2,      // dies on light bumps
    Standard0 = 10,  // medium threshold
    Standard1 = 11,  // medium threshold
    Standard2 = 12,  // medium threshold
    Heavy0 = 20,    // needs a big hit
    Heavy1 = 21,    // needs a big hit
    Heavy2 = 22,    // needs a big hit
    Miniboss = 30,    // needs a big hit
    Boss = 40,    // needs a big hit
    BigBoss = 50,    // needs a big hit
}
